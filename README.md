# Notenest
it's a demonstration PWA (Progressive Web App) page that lets users write and persist notes locally, with a service worker enabling offline access — designed to teach the fundamentals of building offline-capable web apps. It also appears to be styled via an external style.css and has a web app manifest for installability.
